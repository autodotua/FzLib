<UserControl x:Class="FzLib.Control.Picker.ColorPickerTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:flat="clr-namespace:FzLib.Control.FlatStyle"
             xmlns:local="clr-namespace:FzLib.Control.Picker"
             mc:Ignorable="d" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4"/>

            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <flat:TextBox x:Name="txt" Text="{Binding CurrentColor, ElementName=colorPicker, Mode=TwoWay}"/>
        <local:ColorPicker x:Name="colorPicker"  Grid.Column="2" />
    </Grid>
</UserControl>
